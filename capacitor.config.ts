import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mhazz.minesweeperreact",
  appName: "Minesweeper React",
  webDir: "dist",
};

export default config;
